version: 2

models:
  - name: valuation
    description: "Aggregated company valuation data"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
      - name: total_assets
      - name: total_liabilities
      - name: valuation
      - name: ticker
      - name:
